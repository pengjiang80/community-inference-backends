backend_name: MinerU
description: |
  MinerU (https://github.com/opendatalab/mineru) is a document parsing engine that converts complex PDFs into structured, machine-readable formats such as Markdown and JSON.
  It was originally developed during the pre-training of InternLM and is particularly strong at handling formulas, symbols, and structured content commonly found in scientific literature.

  This inference backend image is built on top of the official MinerU Dockerfile:
  https://github.com/opendatalab/MinerU/blob/master/docker/global/Dockerfile

  The backend image includes a built-in model, so the model selected on the Deployments page may be ignored.
health_check_path: /v1/models
default_run_command: mineru-vllm-server --port {{port}} --served-model-name {{model_name}}
version_configs:
  2.7.0:
    image_name: gpustackcommunity/mineru:v2.7.0
    custom_framework: cuda
  3.0.5:
    image_name: gpustackcommunity/mineru:3.0.5
    custom_framework: cuda
  3.0.4-a2:
    image_name: gpustackcommunity/mineru:3.0.4-800I-A2-aarch64
    custom_framework: cann
  3.0.4-a3:
    image_name: gpustackcommunity/mineru:3.0.4-800I-A3-aarch64
    custom_framework: cann
  3.0.4-310p:
    image_name: gpustackcommunity/mineru:3.0.4-300I-Duo-aarch64
    custom_framework: cann
default_version: 3.0.5
